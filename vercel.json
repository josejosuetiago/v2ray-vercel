export default async function handler(req, res) {
  const response = await fetch('http://144.22.155.195:80', {
    method: req.method,
    headers: req.headers,
    body: req.method === 'GET' ? null : req.body,
  });

  const data = await response.text();

  res.status(response.status).send(data);
}
